
/**
 *
 * @param fromTime
 * @param toTime
 */
export async function forTimeInDay(fromTime: ITime, toTime: ITime): Promise<void> {

    const fromTimeToday = createDateWithTime(fromTime);
    const fromTimeToday = createDateWithTime(fromTime);

}

// !!! To samples
// !!! To README

